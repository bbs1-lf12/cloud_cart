<ul class="navbar__menu">
    <li>
        <a href="{{ path('article_list') }}">Store</a>
    </li>
    <li>
        <span class="nav-bar__login relative group">
            <a href="{{ path('cart_show') }}">Cart</a>
            {% if app.user %}
                {{ include('partials/cartDropdown/_userCartDropDown.html.twig') }}
            {% else %}
                {{ include('partials/cartDropdown/_sessionCartDropdown.html.twig') }}
            {% endif %}
            </span>
    </li>
    {% if app.user %}
        <li>
            <a href="{{ path('list_orders') }}">Orders</a>
        </li>
        {% if is_granted('ROLE_ADMIN') %}
            <li>
                <a href="{{ path('admin_index') }}">Admin-Panel</a>
            </li>
        {% endif %}
        <li>
            <a href="{{ path('app_logout') }}">Logout</a>
        </li>
    {% else %}
        <li>
            <span class="nav-bar__login relative group">
                Login
                {{ include('partials/_loginDropdown.html.twig') }}
            </span>
        </li>
        <li>
            <a href="{{ path('app_register') }}">Register</a>
        </li>
    {% endif %}
    <li id="dark-mode" class="icon sun-icon cursor-pointer">
    </li>
</ul>
